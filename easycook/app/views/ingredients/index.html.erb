<p id="notice"><%= notice %></p>
  <div class="row">
      <div class="col-lg-12 text-center">
          <h2 class="section-heading">Ingredientes</h2>
      </div>
  </div>

<%= form_tag ingredients_path, :method => 'get' do %>
  <p>
    <div>
    <div class="col-xs-4 col-xs-offset-4 text-center">
      <div id="search-ingredients">
      <%= text_field_tag :search, params[:search], class: "form-control" %>
      <%= submit_tag "Search", :name => nil, class:"btn btn-xl"%>
    </div>
  </p>
<% end %>


	<div class="row">
        <div class="span12">
    		<table class="table table-condensed table-hover">
    			<thead>
    				<tr>
    					<th> Nome </th>
    					<th> Descrição </th>
    					<th><%=  link_to 'Tags', tags_path %> </th>
    				</tr>
    			</thead>
    			<tbody>
            <% @ingredients.each do |ingredient| %>
    				<tr>
              <td><%= ingredient.name %></td>
              <td><%= raw ingredient.tag_list.map {|t| link_to t, tag_path(t)}.join(', ') %></td>
              <td><%= link_to image_tag("eye.svg", width:24, height:24, border: 0), ingredient_path(ingredient) %></td>
              <td><%= link_to image_tag("settings.svg", width:24, height:24, border: 0), edit_ingredient_path(ingredient) %></td>
              <td><%= link_to image_tag("delete-button.svg", width:24, height:24, border: 0), ingredient, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    				</tr>
            <% end %>
    			</tbody>
    		</table>
    	</div>
	</div>



<%= link_to 'New Ingredient', new_ingredient_path , class:"btn btn-xl"%>
