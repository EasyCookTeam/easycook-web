<section id="loguin">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h2 class="section-heading">Log in</h2>
                <h3 class="section-subheading text-muted">Logue-se para desfrutar dos nossos serviços.</h3>
            </div>
        </div>
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="row">
            <div class="col-lg-12">
                <form name="sentMessage" id="contactForm" novalidate>
                    <div class="row">
                        <div class="col-md-6 col-md-offset-3">
                            <div class="form-group">
                                <%= f.label :email %><br />
                                <%= f.email_field :email, :required => true, autofocus: true, class: "form-control", placeholder: "Coloque aqui seu email *"%>
                                <p class="help-block text-danger"></p>
                            </div>
                            <div class="form-group">
                                <%= f.label :password %><br />
                                <%= f.password_field :password, autocomplete: "off", :required => true, class: "form-control", placeholder: "Coloque aqui sua senha *"%>
                                <p class="help-block text-danger"></p>
                            </div>
                            <div class="form-group">
                              <% if devise_mapping.rememberable? -%>
                                <div class="field">
                                  <%= f.check_box :remember_me %>
                                  <%= f.label :remember_me %>
                                </div>
                              <% end -%>
                            </div>
                        </div>
                        <div class="clearfix"></div>
                        <div class="col-lg-12 text-center", id="success">
                                <%= f.submit "Log in", class: "btn btn-xl"%>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>
<% end %>
<%= render "devise/shared/links" %>
